# Required in production (Render)
BASE_URL=https://your-app.onrender.com
SESSION_SECRET=change-me-long-random

# Optional
TRUST_PROXY=1
# Comma-separated list of allowed RelayState origins for redirects (defends against open-redirect)
ALLOWED_RELAYSTATE_ORIGINS=https://your-app.onrender.com
# Runtime connection TTL in minutes
CONNECTION_TTL_MINUTES=1440
