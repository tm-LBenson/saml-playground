# Port the server listens on
PORT=3000

# Public base URL where this app is reachable from the IdP (must be HTTPS for most IdPs)
# Examples:
#   BASE_URL=https://your-app.example.com
#   BASE_URL=https://<something>.ngrok-free.app
BASE_URL=https://your-public-url.example.com

# Required for sessions (use a long random string in real deployments)
SESSION_SECRET=change-me-please

# Where the SAML connections live
CONNECTIONS_FILE=./connections.json

# If you're behind a reverse proxy / load balancer (Cloud Run, Heroku, etc), set TRUST_PROXY=1
TRUST_PROXY=1

# Optional: comma-separated list of allowed origins for RelayState full URLs.
# (Relative RelayState like "/me" are always allowed.)
ALLOWED_RELAYSTATE_ORIGINS=https://your-public-url.example.com
